{
  "name": "ohttp-client",
  "version": "0.0.0-alpha",
  "private": true,
  "type": "module",
  "description": "Minimal OHTTP client",
  "exports": "./index.js",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --out-name ohttp_client .. -- --features wasm",
    "test": "node e2e.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
